server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  location /api/ { proxy_pass http://backend:8000/api/; }
  location /storage/ { proxy_pass http://backend:8000/storage/; }
  location / { try_files $uri /index.html; }
}
